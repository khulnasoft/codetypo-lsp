[default]
# ignore lines with trailing # skip_me
extend-ignore-re = ["(?Rm)^.*#\\s*skip_me$"]

[default.extend-words]
# tell codetypo which of the several possible corrections to use
fo = "of"

[files]
# ignore codetypo in CHANGELOG.md, integration_test.rs
extend-exclude = ["CHANGELOG.md", "integration_test.rs"]
